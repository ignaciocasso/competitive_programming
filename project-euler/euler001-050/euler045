It's quite easy to check if a number is triangular, pentagonal or hexagonal,
so brute force is enough, probably better to loop over the hexagonals. Also,
H(n)=T(2n-1) so we can skip the triangular check
